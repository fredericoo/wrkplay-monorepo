/** Time a non started match expires. */
export const NON_STARTED_MATCH_EXPIRATION_TIME_MS = 1000 * 60 * 5; // 5 minutes

/** Time an ongoing match expires. */
export const ONGOING_MATCH_EXPIRATION_TIME_MS = 1000 * 60 * 60 * 2; // 2 hours
